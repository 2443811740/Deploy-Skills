# Deployment Session Intake

请一次性确认本次会话的信息；不涉及的部分写“不涉及”，暂时不知道写“未知”。不要填写密码、令牌、私钥或口令。

## 1. 任务范围与版本

- 范围：纯分析 / 修改不构建 / 构建 / BC 参考推理 / 部署 / 板端验证
- 项目根目录：
- 目标分支或 revision：
- 基线版本、目标版本、变更文档：

## 2. 编译与产物

- 编译环境及连接/进入方式：
- 编译前环境准备：
- 工作目录：
- 精确编译命令：
- 预期产物路径：

## 3. Python/BC 参考推理

- 本次是否需要 BC 参考、dump 或代码对比：
- BC 服务器 SSH alias，或非敏感 host/user/port 与认证方式：
- 服务器代码目录、revision 和环境准备：
- BC 推理工作目录和精确命令：
- BC 模型、配置、数据清单/case、固定样本：
- 输出、可视化和 dump 目录：
- 是否允许临时修改 Python 增加 dump/CPU 对照，以及恢复要求：

## 4. 部署与板端环境

- 目标平台/板型：
- 连接方式或 SSH alias（不要填写密码/令牌/私钥）：
- 上传方式、部署文件和目标目录：
- 覆盖许可、备份和回滚要求：
- 服务/进程停止与启动方式：
- 板端环境准备和 runtime/library 变量：
- 模型、配置、标定、数据和运行目录：
- 精确运行命令：

## 5. 验收

- 固定 case/样本/帧范围：
- 输出、日志和可视化目录：
- 正确性/可视化/性能/稳定性通过标准：
- 结果回收与覆盖策略：
